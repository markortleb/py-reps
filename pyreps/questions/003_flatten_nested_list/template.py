def flatten(nested: list) -> list:
    """
    Given a nested list of arbitrary depth, return a flat list of all values.

    Args:
        nested: A list that may contain integers or further nested lists.

    Returns:
        A flat list preserving original order.
    """
    pass
